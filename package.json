{
  "name": "poc-introquiz",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "server": "node server/index.js",
    "client": "vite --config vite.config.ts",
    "dev": "concurrently \"npm run server\" \"npm --workspace=client run dev\"",
    "test": "vitest"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "vitest": "^1.5.2"
  },
  "workspaces": [
    "client",
    "server"
  ]
}
